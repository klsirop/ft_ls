добавить шапки
проверить мэйкфайл на маке
добавить файл автора
прогнать по норминатору
обработать мои ручные ERROR
обработать дофига исключений с поведением флагов
если папка под судо (permission denided)
max_cmd_size


по делу:
посмотреть ft_find_sort_order - done
дописать ft_find_info_type - дописала, но получилось сомнительно
в ft_out_argc не обрабатываются файлы (если аргумент командной строки - файл, считается, что его нет)
дописать ft_sort_names - вроде дописала
что если попросить вывести одинаковые папки или файлы
походу придется делать юнит-тесты, тк код говно

ближайшие планы:
сделать total
сделать вывод по блокам с флагом l
сделать вывод по блокам без флага l
сделать норм вывод с двоеточиями и пробелами с флагом l

потом примерно:
запустить чекер
чекнуть другие флаги


Права: r=4 w=2 x=1
нужно сначала выяснить кто я? :э 
если я не могу читать -permission denided		tmp->rights->usr[0] != 'r'
если я могу читать и исполнять то вывод			tmp->rights->usr[0] == 'r' && tmp->rights->usr[2] == 'x'
в остальных случаях ничего не выводится			else

